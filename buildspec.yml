version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo "Instalando dependências..."
      #- npm install

  build:
    commands:
      - echo "Iniciando compilação..."
      #- npm run build

artifacts:
  files:
    - '**/*'
